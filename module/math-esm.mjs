// const add = (a, b) => {
//   return a + b;
// };

// export default add;

// ------------------------------------------

// export default (a, b) => {
//   return a + b;
// };

// ------------------------------------------

const add = (a, b) => {
  return a + b;
};

const subtract = (a, b) => {
  return a - b;
};

// export default {
//   add,
//   subtract,
// };

// ------------------------------------------

export const multiply = (a, b) => {
  return a * b;
};

export const divide = (a, b) => {
    return a / b;
  };
  